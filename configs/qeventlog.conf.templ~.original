DATABASES = {
  "default": {
     "ENGINE": "django.db.backends.mysql",
     "NAME": "${QLOG.DB.DATABASE_NAME}",
     "HOST": "${QLOG.DB.DATABASE_HOST}",
     "USER": "${QLOG.DB.DATABASE_USER}",
     "PASSWORD": "${QLOG.DB.DATABASE_PASSWD}",
     "OPTIONS": {
     "init_command": "SET storage_engine=INNODB",
    }
  }
}
# http://raven.readthedocs.org/en/latest/config/django.html
RAVEN_CONFIG = {
  "dsn": "sync+${QLOG.SENTRY.DSN}",
}
